# MLHeart
Implemented data preprocessing, feature scaling, and model training using scikit-learn. The application allows users to input data through an interactive UI and instantly receive prediction results. Deployed the project on a cloud platform for public access.
